---
phase: 03-local-tool-implementation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/BFGA.Canvas/Rendering/LocalLaserState.cs
  - src/BFGA.Canvas/Rendering/PingMarkerState.cs
  - src/BFGA.Canvas/Rendering/LaserTrailRenderer.cs
  - tests/BFGA.Canvas.Tests/LocalLaserRendererTests.cs
autonomous: true
requirements:
  - RNDR-01
  - RNDR-04
  - INPT-02

must_haves:
  truths:
    - "Local laser overlay has its own transient state and never touches board elements"
    - "Local dot, trail width, and ping radius stay constant on screen by dividing screen sizes by zoom"
    - "Quick tap ping uses expanding ring plus center dot and fades after roughly 2-3 seconds"
    - "Renderer exposes visibility helpers so BoardCanvas timer can stop when nothing remains visible"
  artifacts:
    - path: "src/BFGA.Canvas/Rendering/LocalLaserState.cs"
      provides: "Transient local laser head and trail state"
      contains: "class LocalLaserState"
    - path: "src/BFGA.Canvas/Rendering/PingMarkerState.cs"
      provides: "Transient ping marker timestamp and position"
      contains: "record PingMarkerState"
    - path: "src/BFGA.Canvas/Rendering/LaserTrailRenderer.cs"
      provides: "Local overlay drawing and fade helpers"
      contains: "DrawLocalLaser"
    - path: "tests/BFGA.Canvas.Tests/LocalLaserRendererTests.cs"
      provides: "Tests for local laser and ping visibility/math"
      contains: "class LocalLaserRendererTests"
  key_links:
    - from: "LocalLaserState"
      to: "LaserTrailBuffer"
      via: "Local state owns a fixed 128-point trail buffer"
      pattern: "new LaserTrailBuffer(128)"
    - from: "LaserTrailRenderer.DrawLocalLaser"
      to: "zoom"
      via: "Constant on-screen sizes computed as screenSize / zoom"
      pattern: "/ zoom"
    - from: "LaserTrailRenderer.DrawPingMarker"
      to: "PingMarkerState"
      via: "Expanding ring plus center dot"
      pattern: "DrawPingMarker"
---

<objective>
Create the local-only laser overlay models and renderer helpers needed for a press-and-hold tool that stays visually correct under pan and zoom.

Purpose: Phase 2 only supports remote trail buffers. Phase 3 needs a local overlay path that can draw a live head dot and a quick-tap ping without persisting anything into board state.

Output: `LocalLaserState`, `PingMarkerState`, and `LaserTrailRenderer` methods for local laser, ping, and visibility checks, with tests that lock timing and zoom math.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-local-tool-implementation/03-CONTEXT.md
@.planning/phases/03-local-tool-implementation/03-UI-SPEC.md
@.planning/phases/02-trail-buffer-renderer/02-02-SUMMARY.md

<interfaces>
From src/BFGA.Canvas/Rendering/RemoteLaserState.cs:
```csharp
public sealed class RemoteLaserState
{
    public LaserTrailBuffer Trail { get; }
    public bool IsActive { get; set; }
    public long LastUpdateMs { get; set; }
    public SKColor Color { get; set; }
}
```

From src/BFGA.Canvas/Rendering/LaserTrailRenderer.cs:
```csharp
public static void DrawLaserTrails(SKCanvas canvas, IReadOnlyDictionary<Guid, RemoteLaserState>? lasers, long currentTimestampMs)
public static bool HasVisibleTrails(IReadOnlyDictionary<Guid, RemoteLaserState>? lasers, long currentTimestampMs, float decayMs = 1500f)
```

From .planning/phases/03-local-tool-implementation/03-UI-SPEC.md:
```text
Laser dot radius: 5px on screen
Trail thickness: 3px on screen at head
Ping center dot radius: 4px on screen
Ping ring expands from 10px to 24px on screen
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add local laser and ping render primitives with deterministic tests</name>
  <files>
    src/BFGA.Canvas/Rendering/LocalLaserState.cs,
    src/BFGA.Canvas/Rendering/PingMarkerState.cs,
    src/BFGA.Canvas/Rendering/LaserTrailRenderer.cs,
    tests/BFGA.Canvas.Tests/LocalLaserRendererTests.cs
  </files>
  <read_first>
    - src/BFGA.Canvas/Rendering/LaserTrailBuffer.cs (fixed-size trail buffer contract)
    - src/BFGA.Canvas/Rendering/RemoteLaserState.cs (existing transient laser shape)
    - src/BFGA.Canvas/Rendering/LaserTrailRenderer.cs (existing trail fade logic)
    - .planning/phases/03-local-tool-implementation/03-UI-SPEC.md (exact dot, trail, and ping screen sizes)
  </read_first>
  <behavior>
    - Test 1: active local laser with a fresh point is visible
    - Test 2: ended local laser with only expired points is not visible
    - Test 3: active ping is visible for its lifetime, then disappears
    - Test 4: `GetWorldSize(5f, 2f)` returns `2.5f` so screen-size visuals stay constant at zoom 2x
    - Test 5: ping progress maps 0% -> 10px ring and 100% -> 24px ring before fade reaches zero
  </behavior>
  <action>
    **RED phase:** Create `tests/BFGA.Canvas.Tests/LocalLaserRendererTests.cs` with xUnit tests that exercise these exact public methods on `LaserTrailRenderer`:

    ```csharp
    public static float GetWorldSize(float screenSize, float zoom)
    public static bool HasVisibleLocalLaser(LocalLaserState? laser, long currentTimestampMs, float decayMs = 1500f)
    public static bool HasVisiblePing(PingMarkerState? ping, long currentTimestampMs, float lifetimeMs = 2400f)
    public static float GetPingRingScreenRadius(long startedAtMs, long currentTimestampMs, float lifetimeMs = 2400f)
    ```

    Use exact assertions:
    - `Assert.Equal(2.5f, LaserTrailRenderer.GetWorldSize(5f, 2f), 3)`
    - `Assert.Equal(10f, LaserTrailRenderer.GetPingRingScreenRadius(1000, 1000), 3)`
    - `Assert.Equal(24f, LaserTrailRenderer.GetPingRingScreenRadius(1000, 3400), 3)`

    **GREEN phase:**

    1. Create `src/BFGA.Canvas/Rendering/LocalLaserState.cs`:
       - `public sealed class LocalLaserState`
       - properties: `LaserTrailBuffer Trail`, `Vector2 HeadPosition`, `bool IsActive`, `long LastUpdateMs`, `SKColor Color`
       - constructor: `LocalLaserState(SKColor color)` initializes `Trail = new LaserTrailBuffer(128)` and default inactive state
       - method: `public void Reset()` clears trail and sets `IsActive = false`

    2. Create `src/BFGA.Canvas/Rendering/PingMarkerState.cs`:
       - `public sealed record PingMarkerState(Vector2 Position, long StartedAtMs, SKColor Color);`

    3. Extend `src/BFGA.Canvas/Rendering/LaserTrailRenderer.cs` with these constants and methods:
       - `private const float LocalDotScreenRadius = 5f;`
       - `private const float LocalTrailScreenWidth = 3f;`
       - `private const float PingCenterDotScreenRadius = 4f;`
       - `private const float PingRingStartScreenRadius = 10f;`
       - `private const float PingRingEndScreenRadius = 24f;`
       - `private const float DefaultPingLifetimeMs = 2400f;`
       - `public static float GetWorldSize(float screenSize, float zoom) => zoom <= 0f ? screenSize : screenSize / zoom;`
       - `public static bool HasVisibleLocalLaser(...)` should return true when the laser is active or any trail point age is below decay
       - `public static bool HasVisiblePing(...)` should return true while `currentTimestampMs - ping.StartedAtMs < lifetimeMs`
       - `public static float GetPingRingScreenRadius(...)` should linearly interpolate from 10f to 24f over the ping lifetime

    4. Add local draw methods using the exact signatures below:
       ```csharp
       public static void DrawLocalLaser(SKCanvas canvas, LocalLaserState? laser, long currentTimestampMs, float zoom)
       public static void DrawPingMarker(SKCanvas canvas, PingMarkerState? ping, long currentTimestampMs, float zoom)
       ```

       `DrawLocalLaser` must:
       - reuse the existing trail fade curve `1f - (t * t)`
       - set trail `StrokeWidth = GetWorldSize(3f, zoom)`
       - draw the head dot at `laser.HeadPosition` with radius `GetWorldSize(5f, zoom)` while `laser.IsActive`

       `DrawPingMarker` must:
       - compute progress from `ping.StartedAtMs`
       - draw one ring with radius `GetWorldSize(GetPingRingScreenRadius(...), zoom)`
       - draw one center dot with radius `GetWorldSize(4f, zoom)`
       - fade alpha to zero by the end of the 2400ms lifetime

    5. Run `dotnet test tests/BFGA.Canvas.Tests --filter "FullyQualifiedName~LocalLaserRendererTests" --no-restore -v q` and keep the tests green.
  </action>
  <verify>
    <automated>dotnet test tests/BFGA.Canvas.Tests --filter "FullyQualifiedName~LocalLaserRendererTests" --no-restore -v q</automated>
  </verify>
  <acceptance_criteria>
    - LocalLaserState.cs contains `public sealed class LocalLaserState`
    - LocalLaserState.cs contains `public LaserTrailBuffer Trail { get; }`
    - PingMarkerState.cs contains `public sealed record PingMarkerState(Vector2 Position, long StartedAtMs, SKColor Color)`
    - LaserTrailRenderer.cs contains `public static float GetWorldSize(float screenSize, float zoom)`
    - LaserTrailRenderer.cs contains `public static void DrawLocalLaser(SKCanvas canvas, LocalLaserState? laser, long currentTimestampMs, float zoom)`
    - LaserTrailRenderer.cs contains `public static void DrawPingMarker(SKCanvas canvas, PingMarkerState? ping, long currentTimestampMs, float zoom)`
    - LaserTrailRenderer.cs contains `GetWorldSize(3f, zoom)` and `GetWorldSize(5f, zoom)`
    - LaserTrailRenderer.cs contains `PingRingStartScreenRadius = 10f` and `PingRingEndScreenRadius = 24f`
    - LocalLaserRendererTests.cs contains `GetWorldSize_ConvertsScreenPixelsUsingZoom`
    - `dotnet test tests/BFGA.Canvas.Tests --filter "FullyQualifiedName~LocalLaserRendererTests" --no-restore -v q` exits 0
  </acceptance_criteria>
  <done>Local laser and ping render primitives exist, use constant on-screen sizes, and have deterministic tests for zoom and lifetime math.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BoardView input -> local overlay models | High-frequency pointer updates feed transient visual state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Denial of Service | LocalLaserState trail | mitigate | Reuse `LaserTrailBuffer(128)` so high-frequency local updates cannot allocate unbounded memory |
| T-03-02 | Tampering | Local ping state | accept | Ping state is local-only and ephemeral; bad values affect only a transient overlay |
</threat_model>

<verification>
1. `dotnet test tests/BFGA.Canvas.Tests --filter "FullyQualifiedName~LocalLaserRendererTests" --no-restore -v q`
2. `dotnet build src/BFGA.Canvas --no-restore -v q`
3. grep `GetWorldSize(3f, zoom)` in `src/BFGA.Canvas/Rendering/LaserTrailRenderer.cs`
4. grep `PingRingStartScreenRadius = 10f` in `src/BFGA.Canvas/Rendering/LaserTrailRenderer.cs`
</verification>

<success_criteria>
- Local overlay state exists separately from remote laser dictionaries
- Local dot, trail, and ping scale from screen pixels to world size using zoom
- Ping lifetime is bounded to roughly 2.4 seconds and fades out completely
- Deterministic tests cover visibility and zoom math
</success_criteria>

<output>
After completion, create `.planning/phases/03-local-tool-implementation/03-01-SUMMARY.md`
</output>
