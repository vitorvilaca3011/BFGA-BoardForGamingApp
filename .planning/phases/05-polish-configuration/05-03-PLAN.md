---
phase: 05-polish-configuration
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/BFGA.App/Views/BoardView.axaml.cs
  - src/BFGA.Canvas/LaserOverlayCanvas.cs
  - tests/BFGA.App.Tests/BoardViewPipelineTests.cs
  - tests/BFGA.Canvas.Tests/LaserOverlayCanvasTests.cs
autonomous: true
requirements:
  - CONF-01

must_haves:
  truths:
    - "Local laser visuals use the persisted preferred presence color instead of the drawing stroke color"
    - "Switching away from Laser Pointer still sends inactive/deactivation behavior and preserves local fade-out"
    - "Remote lasers with no updates for ~3 seconds become synthetic releases and then fade naturally"
  artifacts:
    - path: "src/BFGA.App/Views/BoardView.axaml.cs"
      provides: "Local laser color source and tool-switch deactivation"
      contains: "LaserPresenceColor"
    - path: "src/BFGA.Canvas/LaserOverlayCanvas.cs"
      provides: "Overlay-driven stale timeout enforcement"
      contains: "3000"
  key_links:
    - from: "BoardView.BeginLocalLaser"
      to: "MainViewModel.LaserPresenceColor"
      via: "local overlay initialization"
      pattern: "LaserPresenceColor"
    - from: "LaserOverlayCanvas.OnLaserFadeTick"
      to: "RemoteLaserState.LastUpdateMs"
      via: "stale timeout synthetic release"
      pattern: "LastUpdateMs"
---

<objective>
Swap local laser rendering to the persisted preferred presence color and enforce stale-timeout release semantics in the laser overlay per D-03, D-06, D-07, D-09, and D-10.

Purpose: Phase 5 is incomplete until the chosen settings color actually drives local laser visuals and silent remote lasers stop behaving like permanently active state.

Output: BoardView uses the persisted presence color for local laser state and `LaserOverlayCanvas` enforces ~3s stale timeout with fade-safe synthetic release behavior.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-configuration/05-CONTEXT.md
@.planning/phases/05-polish-configuration/05-UI-SPEC.md
@.planning/phases/05-polish-configuration/05-RESEARCH.md
@.planning/phases/03-local-tool-implementation/03-CONTEXT.md
@.planning/phases/04-multiplayer-integration/04-03-SUMMARY.md

<interfaces>
From src/BFGA.App/Views/BoardView.axaml.cs:
```csharp
private void BeginLocalLaser(Vector2 boardPoint, Point screenPoint, long timestampMs)
private void CancelLocalLaser()
```

From src/BFGA.Canvas/LaserOverlayCanvas.cs:
```csharp
private void OnLaserFadeTick(object? sender, EventArgs e)
public IReadOnlyDictionary<Guid, RemoteLaserState>? RemoteLasers { get; set; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: make local laser use persisted presence color and preserve tool-switch deactivation semantics</name>
  <files>tests/BFGA.App.Tests/BoardViewPipelineTests.cs, src/BFGA.App/Views/BoardView.axaml.cs</files>
  <read_first>
    - src/BFGA.App/Views/BoardView.axaml.cs
    - src/BFGA.App/ViewModels/MainViewModel.cs
    - tests/BFGA.App.Tests/BoardViewPipelineTests.cs
    - .planning/phases/05-polish-configuration/05-CONTEXT.md
    - .planning/phases/03-local-tool-implementation/03-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: `BeginLocalLaser` uses `MainViewModel.LaserPresenceColor`, not `SelectedStrokeColor`.
    - Test 2: switching away from Laser Pointer still triggers inactive publish and leaves local trail intact for fade-out.
    - Test 3: quick tap ping continues using the same chosen presence color.
  </behavior>
  <action>
    Add failing tests with these exact names:
    - `LaserPointer_BeginLocalLaser_UsesLaserPresenceColorInsteadOfSelectedStrokeColor`
    - `LaserPointer_ToolSwitchAway_PublishesInactiveOperationAndPreservesTrail`
    - `LaserPointer_QuickTapPing_UsesLaserPresenceColor`

    Then update `src/BFGA.App/Views/BoardView.axaml.cs` exactly:
    - in `BeginLocalLaser(...)`, replace `_boardScreenViewModel?.SelectedStrokeColor` with `_boardScreenViewModel?.MainViewModel.LaserPresenceColor`
    - keep `CancelLocalLaser()` publish behavior exactly on tool switch / pointer exit / capture loss
    - do not clear local trail buffer on cancel or release
    - do not read `SelectedStrokeColor` anywhere in local laser lifecycle helpers after this change

    Implement per D-03, D-09, and D-10: persistent presence color drives local laser visuals while cancellation still behaves like release for fade cleanup.
  </action>
  <verify>
    <automated>dotnet test tests/BFGA.App.Tests --filter "FullyQualifiedName~BoardViewPipelineTests" --no-restore -v q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/BFGA.App.Tests/BoardViewPipelineTests.cs` contains `LaserPointer_BeginLocalLaser_UsesLaserPresenceColorInsteadOfSelectedStrokeColor`
    - `tests/BFGA.App.Tests/BoardViewPipelineTests.cs` contains `LaserPointer_ToolSwitchAway_PublishesInactiveOperationAndPreservesTrail`
    - `src/BFGA.App/Views/BoardView.axaml.cs` contains `MainViewModel.LaserPresenceColor`
    - `src/BFGA.App/Views/BoardView.axaml.cs` does not contain `SelectedStrokeColor ?? SkiaSharp.SKColors.White` inside `BeginLocalLaser`
    - `dotnet test tests/BFGA.App.Tests --filter "FullyQualifiedName~BoardViewPipelineTests" --no-restore -v q` exits 0
  </acceptance_criteria>
  <done>Local laser visuals come from the persisted preferred presence color and tool-switch cancellation still sends fade-safe deactivation.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: enforce stale remote timeout as synthetic release in laser overlay</name>
  <files>tests/BFGA.Canvas.Tests/LaserOverlayCanvasTests.cs, src/BFGA.Canvas/LaserOverlayCanvas.cs</files>
  <read_first>
    - src/BFGA.Canvas/LaserOverlayCanvas.cs
    - src/BFGA.Canvas/Rendering/RemoteLaserState.cs
    - src/BFGA.Canvas/Rendering/LaserTrailRenderer.cs
    - tests/BFGA.Canvas.Tests/LaserOverlayCanvasTests.cs
    - .planning/phases/05-polish-configuration/05-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: remote active laser older than ~3000ms becomes inactive during overlay tick.
    - Test 2: stale timeout refreshes the last trail-point timestamp once so the trail gets one normal fade window after the synthetic release.
    - Test 3: non-stale remote lasers are left unchanged.
  </behavior>
  <action>
    Add failing tests with these exact names:
    - `LaserOverlayCanvas_StaleActiveRemoteLaser_BecomesInactiveAfterThreeSeconds`
    - `LaserOverlayCanvas_StaleTimeout_RefreshesLastPointForFadeOut`
    - `LaserOverlayCanvas_FreshRemoteLaser_RemainsActive`

    Then update `src/BFGA.Canvas/LaserOverlayCanvas.cs` exactly:
    - add `private const long RemoteLaserStaleTimeoutMs = 3000;`
    - add private helper:
      ```csharp
      private bool ReleaseStaleRemoteLasers(long now)
      ```
    - helper behavior for each `RemoteLaserState` in `RemoteLasers`:
      - if `state.IsActive` is false, skip
      - if `now - state.LastUpdateMs < RemoteLaserStaleTimeoutMs`, skip
      - else set `state.IsActive = false`
      - if `state.Trail.Count > 0`, call `state.Trail.UpdateLast(last.Position, now)` on the current last point so timeout release gets the same fade window as a normal remote release
      - return whether any state changed
    - call `ReleaseStaleRemoteLasers(now)` at the start of `OnLaserFadeTick(...)` before visibility checks
    - if stale release changed any state, keep the existing overlay invalidation/timer path running; do not touch `BoardCanvas`

    Implement per D-06/D-07: about 3 seconds of silence forces inactive state, then existing trail fades naturally instead of being hard-deleted.
  </action>
  <verify>
    <automated>dotnet test tests/BFGA.Canvas.Tests --filter "FullyQualifiedName~LaserOverlayCanvasTests" --no-restore -v q</automated>
  </verify>
  <acceptance_criteria>
    - `src/BFGA.Canvas/LaserOverlayCanvas.cs` contains `RemoteLaserStaleTimeoutMs = 3000`
    - `src/BFGA.Canvas/LaserOverlayCanvas.cs` contains `private bool ReleaseStaleRemoteLasers(long now)`
    - `src/BFGA.Canvas/LaserOverlayCanvas.cs` contains `state.IsActive = false`
    - `src/BFGA.Canvas/LaserOverlayCanvas.cs` contains `state.Trail.UpdateLast`
    - `tests/BFGA.Canvas.Tests/LaserOverlayCanvasTests.cs` contains `LaserOverlayCanvas_StaleActiveRemoteLaser_BecomesInactiveAfterThreeSeconds`
    - `dotnet test tests/BFGA.Canvas.Tests --filter "FullyQualifiedName~LaserOverlayCanvasTests" --no-restore -v q` exits 0
  </acceptance_criteria>
  <done>Silent remote lasers time out after ~3 seconds through a synthetic release path that preserves the normal fade-out behavior and overlay isolation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted color -> local overlay | configured identity color becomes live local pointer visuals |
| remote laser state -> overlay timeout logic | stale peer state is mutated inside laser-only render timer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Tampering | `BoardView.BeginLocalLaser` | mitigate | source color only from `MainViewModel.LaserPresenceColor`, never `SelectedStrokeColor`, so drawing state cannot silently override configured presence identity |
| T-05-08 | Denial of Service | `LaserOverlayCanvas` stale remote state | mitigate | release stale active lasers after 3000ms and keep timeout handling in overlay-only timer so board redraw path stays isolated |
</threat_model>

<verification>
1. `dotnet test tests/BFGA.App.Tests --filter "FullyQualifiedName~BoardViewPipelineTests" --no-restore -v q`
2. `dotnet test tests/BFGA.Canvas.Tests --filter "FullyQualifiedName~LaserOverlayCanvasTests" --no-restore -v q`
3. grep `MainViewModel.LaserPresenceColor` in `src/BFGA.App/Views/BoardView.axaml.cs`
4. grep `RemoteLaserStaleTimeoutMs = 3000` in `src/BFGA.Canvas/LaserOverlayCanvas.cs`
</verification>

<success_criteria>
- Local laser color no longer depends on drawing stroke color
- Tool-switch deactivation still uses release-equivalent fade semantics
- Silent remote lasers self-release after ~3 seconds and fade naturally
- Timeout handling remains isolated to laser overlay rendering path
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-configuration/05-03-SUMMARY.md`
</output>
