---
phase: 05-polish-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/BFGA.App/Services/SettingsService.cs
  - src/BFGA.App/ViewModels/MainViewModel.cs
  - src/BFGA.App/ViewModels/BoardScreenViewModel.cs
  - src/BFGA.App/Views/SettingsPanel.axaml
  - src/BFGA.App/Views/SettingsPanel.axaml.cs
  - tests/BFGA.App.Tests/MainViewModelTests.cs
  - tests/BFGA.App.Tests/BoardScreenViewModelTests.cs
  - tests/BFGA.App.Tests/PropertyPanelTests.cs
autonomous: true
requirements:
  - CONF-01

must_haves:
  truths:
    - "User can open Settings and see a dedicated LASER COLOR section with a current-color preview and 16 swatches"
    - "Changing the setting updates one persisted preferred presence color, not the drawing stroke color"
    - "Chosen color survives app restart through settings.json"
  artifacts:
    - path: "src/BFGA.App/Services/SettingsService.cs"
      provides: "Persisted preferred presence color in settings.json"
      contains: "LaserPresenceColorHex"
    - path: "src/BFGA.App/Views/SettingsPanel.axaml"
      provides: "Settings-panel-only LASER COLOR UI"
      contains: "LASER COLOR"
    - path: "src/BFGA.App/ViewModels/MainViewModel.cs"
      provides: "Runtime preferred presence color property"
      contains: "LaserPresenceColor"
  key_links:
    - from: "SettingsPanel.axaml.cs"
      to: "BoardScreenViewModel"
      via: "swatch click handlers"
      pattern: "LaserPresenceColor"
    - from: "MainViewModel"
      to: "SettingsService"
      via: "load/save preferred color"
      pattern: "LaserPresenceColorHex"
---

<objective>
Create the preferred presence-color contract and settings-panel editor so Phase 5 has one persistent, settings-panel-only source of truth for laser color per D-01 to D-05 and the UI contract.

Purpose: Phase 5 cannot satisfy shared presence identity or restart persistence until the preferred color exists outside `SelectedStrokeColor` and can be edited from the approved settings UI.

Output: persisted `LaserPresenceColor` contract in runtime/settings plus a `LASER COLOR` settings section using the approved 4x4 swatch UI.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-polish-configuration/05-CONTEXT.md
@.planning/phases/05-polish-configuration/05-UI-SPEC.md
@.planning/phases/05-polish-configuration/05-RESEARCH.md
@src/BFGA.App/Views/PropertyPanel.axaml

<interfaces>
From src/BFGA.App/Services/SettingsService.cs:
```csharp
public float GridOpacity { get; set; }
public void Load()
public void SaveDebounced()
```

From src/BFGA.App/ViewModels/MainViewModel.cs:
```csharp
public float GridOpacity { get; set; }
public bool IsSettingsOpen { get; set; }
```

From src/BFGA.App/ViewModels/BoardScreenViewModel.cs:
```csharp
public MainViewModel MainViewModel { get; }
public RelayCommand ToggleSettingsCommand { get; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: add persisted preferred presence-color contract and viewmodel surface</name>
  <files>tests/BFGA.App.Tests/MainViewModelTests.cs, tests/BFGA.App.Tests/BoardScreenViewModelTests.cs, src/BFGA.App/Services/SettingsService.cs, src/BFGA.App/ViewModels/MainViewModel.cs, src/BFGA.App/ViewModels/BoardScreenViewModel.cs</files>
  <read_first>
    - src/BFGA.App/Services/SettingsService.cs
    - src/BFGA.App/ViewModels/MainViewModel.cs
    - src/BFGA.App/ViewModels/BoardScreenViewModel.cs
    - tests/BFGA.App.Tests/MainViewModelTests.cs
    - tests/BFGA.App.Tests/BoardScreenViewModelTests.cs
    - .planning/phases/05-polish-configuration/05-CONTEXT.md
    - .planning/phases/05-polish-configuration/05-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: settings load/save round-trips a preferred presence color hex string through `settings.json` data model.
    - Test 2: `MainViewModel` exposes `LaserPresenceColor` loaded from settings and saves it when changed.
    - Test 3: `BoardScreenViewModel` exposes a proxy property for settings-panel binding and raises property-changed notifications.
    - Test 4: changing `LaserPresenceColor` does not mutate `SelectedStrokeColor`.
  </behavior>
  <action>
    Add failing tests with these exact names:
    - `MainViewModel_LoadsLaserPresenceColorFromSettingsService`
    - `MainViewModel_ChangingLaserPresenceColor_PersistsToSettingsService`
    - `BoardScreenViewModel_LaserPresenceColor_ProxiesMainViewModel`
    - `BoardScreenViewModel_LaserPresenceColor_DoesNotChangeSelectedStrokeColor`

    Then add the preferred presence-color contract exactly:
    1. In `src/BFGA.App/Services/SettingsService.cs`
       - add `public string LaserPresenceColorHex { get; set; } = "#FFFFFF";`
       - load it from `SettingsData.LaserPresenceColorHex`, default `#FFFFFF`
       - save it back into `SettingsData`
       - add matching property on nested `SettingsData`

    2. In `src/BFGA.App/ViewModels/MainViewModel.cs`
       - add `private SkiaSharp.SKColor _laserPresenceColor = SkiaSharp.SKColors.White;`
       - after `_settingsService.Load();`, initialize `_laserPresenceColor = SkiaSharp.SKColor.Parse(_settingsService.LaserPresenceColorHex);`
       - add public property:
         ```csharp
         public SkiaSharp.SKColor LaserPresenceColor
         ```
       - setter must:
         - call `SetProperty`
         - store `_settingsService.LaserPresenceColorHex = value.ToString()`
         - call `_settingsService.SaveDebounced()`
       - do not mutate `GridOpacity`, `SelectedStrokeColor`, or any drawing-property field when this changes

    3. In `src/BFGA.App/ViewModels/BoardScreenViewModel.cs`
       - add public proxy property:
         ```csharp
         public SKColor LaserPresenceColor
         {
             get => MainViewModel.LaserPresenceColor;
             set => MainViewModel.LaserPresenceColor = value;
         }
         ```
       - when `MainViewModel` raises `nameof(MainViewModel.LaserPresenceColor)`, raise `nameof(LaserPresenceColor)` from `BoardScreenViewModel`

    Implement per D-01 to D-05: one persistent preferred presence color, separate from drawing color, stored in settings.json.
  </action>
  <verify>
    <automated>dotnet test tests/BFGA.App.Tests --filter "FullyQualifiedName~MainViewModelTests|FullyQualifiedName~BoardScreenViewModelTests" --no-restore -v q</automated>
  </verify>
  <acceptance_criteria>
    - `src/BFGA.App/Services/SettingsService.cs` contains `LaserPresenceColorHex`
    - `src/BFGA.App/ViewModels/MainViewModel.cs` contains `public SkiaSharp.SKColor LaserPresenceColor`
    - `src/BFGA.App/ViewModels/MainViewModel.cs` contains `_settingsService.LaserPresenceColorHex = value.ToString()`
    - `src/BFGA.App/ViewModels/BoardScreenViewModel.cs` contains `public SKColor LaserPresenceColor`
    - `dotnet test tests/BFGA.App.Tests --filter "FullyQualifiedName~MainViewModelTests|FullyQualifiedName~BoardScreenViewModelTests" --no-restore -v q` exits 0
  </acceptance_criteria>
  <done>Preferred presence color exists as persisted runtime state and can be consumed by later plans without touching drawing-color state.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: add settings-panel-only LASER COLOR swatch UI</name>
  <files>tests/BFGA.App.Tests/PropertyPanelTests.cs, src/BFGA.App/Views/SettingsPanel.axaml, src/BFGA.App/Views/SettingsPanel.axaml.cs</files>
  <read_first>
    - src/BFGA.App/Views/SettingsPanel.axaml
    - src/BFGA.App/Views/SettingsPanel.axaml.cs
    - src/BFGA.App/Views/PropertyPanel.axaml
    - src/BFGA.App/Styles/WhiteboardTheme.axaml
    - tests/BFGA.App.Tests/PropertyPanelTests.cs
    - .planning/phases/05-polish-configuration/05-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: settings panel AXAML contains a visible `LASER COLOR` section label and helper text naming roster/cursor/laser scope.
    - Test 2: settings panel contains a 4x4 grid of the exact 16 swatches already used by `PropertyPanel.axaml`.
    - Test 3: swatch click handler updates `BoardScreenViewModel.LaserPresenceColor`.
    - Test 4: no toolbar picker or duplicate editor is introduced.
  </behavior>
  <action>
    Add failing tests with these exact names:
    - `SettingsPanel_ContainsLaserColorSectionAndHelperCopy`
    - `SettingsPanel_ContainsExpectedLaserColorSwatches`
    - `SettingsPanelSwatchClick_UpdatesLaserPresenceColor`

    Then update settings UI exactly per D-04/D-05 and UI-SPEC:
    1. In `src/BFGA.App/Views/SettingsPanel.axaml`
       - add a new section between Grid Opacity and placeholder sections with:
         - heading text `LASER COLOR`
         - helper text `Used for roster, cursor, and laser`
         - one 24x24 preview `Border` bound to `LaserPresenceColor` through `SKColorToBrushConverter`
         - one `WrapPanel` with 16 `Button Classes="color-swatch"` swatches using these exact `Tag`/`Background` values:
           `#000000`, `#FFFFFF`, `#FF3B30`, `#FF9500`, `#FFCC00`, `#34C759`, `#007AFF`, `#AF52DE`, `#5856D6`, `#FF2D55`, `#A2845E`, `#8E8E93`, `#636366`, `#48484A`, `#2C2C2E`, `#1C1C1E`
         - selected-state visual using 2px `AccentWhite` ring on the current swatch; implement via style selector or direct `BorderThickness`/class logic, but keep swatch size 24x24 and no extra glow
       - do not add toolbar buttons, flyouts, or duplicate color editors

    2. In `src/BFGA.App/Views/SettingsPanel.axaml.cs`
       - add `OnLaserColorSwatchClick(object? sender, RoutedEventArgs e)`
       - parse button `Tag` with `SKColor.Parse`
       - assign `((BoardScreenViewModel)DataContext).LaserPresenceColor`

    Reuse existing `color-swatch` pattern from `PropertyPanel.axaml`. Keep section width and panel shell unchanged.
  </action>
  <verify>
    <automated>dotnet test tests/BFGA.App.Tests --filter "FullyQualifiedName~PropertyPanelTests|FullyQualifiedName~BoardScreenViewModelTests" --no-restore -v q</automated>
  </verify>
  <acceptance_criteria>
    - `src/BFGA.App/Views/SettingsPanel.axaml` contains `LASER COLOR`
    - `src/BFGA.App/Views/SettingsPanel.axaml` contains `Used for roster, cursor, and laser`
    - `src/BFGA.App/Views/SettingsPanel.axaml` contains each of `#000000`, `#FFFFFF`, `#FF3B30`, `#007AFF`, `#2C2C2E`, `#1C1C1E`
    - `src/BFGA.App/Views/SettingsPanel.axaml.cs` contains `OnLaserColorSwatchClick`
    - `src/BFGA.App/Views/ToolBar.axaml` does not contain `LASER COLOR`
    - `dotnet test tests/BFGA.App.Tests --filter "FullyQualifiedName~PropertyPanelTests|FullyQualifiedName~BoardScreenViewModelTests" --no-restore -v q` exits 0
  </acceptance_criteria>
  <done>Settings panel is the only color editor, uses approved copy/layout, and writes the preferred presence color through the new viewmodel contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings UI -> persisted config | user input becomes stored runtime identity state |
| persisted config -> runtime color | unvalidated config text becomes `SKColor` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | `SettingsService.Load()` | mitigate | default invalid or missing `LaserPresenceColorHex` to `#FFFFFF` and parse through a single `SKColor.Parse`/fallback path in `MainViewModel` |
| T-05-02 | Spoofing | duplicate UI editors | mitigate | keep color editor only in `SettingsPanel`; do not add toolbar or drawing-color coupling |
| T-05-03 | Repudiation | settings-panel state changes | accept | local desktop settings change is low-risk; persistence through existing debounced save path is sufficient |
</threat_model>

<verification>
1. `dotnet test tests/BFGA.App.Tests --filter "FullyQualifiedName~MainViewModelTests|FullyQualifiedName~BoardScreenViewModelTests|FullyQualifiedName~PropertyPanelTests" --no-restore -v q`
2. grep `LaserPresenceColorHex` in `src/BFGA.App/Services/SettingsService.cs`
3. grep `LASER COLOR` in `src/BFGA.App/Views/SettingsPanel.axaml`
4. confirm `src/BFGA.App/Views/ToolBar.axaml` has no new laser color picker markup
</verification>

<success_criteria>
- Preferred presence color persists through `settings.json`
- Settings panel is sole editing surface for the color
- Runtime contract exists for later multiplayer + rendering plans
- Drawing stroke color remains independent from laser/presence color
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-configuration/05-01-SUMMARY.md`
</output>
