---
phase: 05-polish-configuration
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/BFGA.Network/Protocol/BoardOperation.cs
  - src/BFGA.Network/GameHost.cs
  - src/BFGA.App/ViewModels/MainViewModel.cs
  - tests/BFGA.Network.Tests/ProtocolTests.cs
  - tests/BFGA.Network.Tests/NetworkTests.cs
  - tests/BFGA.App.Tests/MainViewModelTests.cs
  - tests/BFGA.App.Tests/RosterOverlayTests.cs
autonomous: true
requirements:
  - CONF-01

must_haves:
  truths:
    - "Changing preferred color while connected updates the same peer-visible identity used by roster, cursor, and remote laser"
    - "Client color changes remain host-authoritative, not payload-authoritative"
    - "Connected clients learn the host's chosen presence color instead of falling back to white for SenderId Guid.Empty"
    - "Peer disconnect still removes laser state immediately"
  artifacts:
    - path: "src/BFGA.Network/Protocol/BoardOperation.cs"
      provides: "Client-to-host presence-color update operation"
      contains: "class UpdatePresenceColorOperation"
    - path: "src/BFGA.Network/GameHost.cs"
      provides: "Host-authoritative player-color update and metadata broadcast"
      contains: "UpdatePresenceColorOperation"
    - path: "src/BFGA.App/ViewModels/MainViewModel.cs"
      provides: "Runtime sync between preferred color and multiplayer identity"
      contains: "SyncPreferredPresenceColor"
  key_links:
    - from: "MainViewModel.LaserPresenceColor"
      to: "Client.SendOperation"
      via: "UpdatePresenceColorOperation when connected as client"
      pattern: "new UpdatePresenceColorOperation"
    - from: "GameHost"
      to: "PeerJoinedOperation"
      via: "metadata upsert broadcast after color change"
      pattern: "new PeerJoinedOperation"
---

<objective>
Propagate the preferred presence color across multiplayer identity so settings changes immediately recolor roster, cursor, and remote laser state per D-01 to D-03 and D-08.

Purpose: settings persistence alone is insufficient. Without a host-authoritative sync path, peers never see the configured shared presence color and host identity remains colorless on remote clients.

Output: a host-authoritative color-update protocol plus `MainViewModel` wiring that converges local preference into peer-visible roster/cursor/laser identity.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-polish-configuration/05-CONTEXT.md
@.planning/phases/05-polish-configuration/05-RESEARCH.md
@.planning/phases/04-multiplayer-integration/04-02-SUMMARY.md

<interfaces>
From src/BFGA.Network/Protocol/BoardOperation.cs:
```csharp
public class PeerJoinedOperation : BoardOperation
{
    [Key(3)] public Guid ClientId { get; set; }
    [Key(4)] public string DisplayName { get; set; }
    [Key(5)] public SKColor AssignedColor { get; set; }
}
```

From src/BFGA.App/ViewModels/MainViewModel.cs:
```csharp
private void ApplyInboundOperation(BoardOperation operation)
private void UpsertRosterEntry(Guid clientId, PlayerInfo playerInfo)
private void RemoveRosterEntry(Guid clientId)
public SkiaSharp.SKColor LaserPresenceColor { get; set; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: add host-authoritative presence-color update protocol</name>
  <files>tests/BFGA.Network.Tests/ProtocolTests.cs, tests/BFGA.Network.Tests/NetworkTests.cs, src/BFGA.Network/Protocol/BoardOperation.cs, src/BFGA.Network/GameHost.cs</files>
  <read_first>
    - src/BFGA.Network/Protocol/BoardOperation.cs
    - src/BFGA.Network/GameHost.cs
    - tests/BFGA.Network.Tests/ProtocolTests.cs
    - tests/BFGA.Network.Tests/NetworkTests.cs
    - .planning/phases/05-polish-configuration/05-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `UpdatePresenceColorOperation` round-trips through MessagePack.
    - Test 2: host accepts the operation from a connected client, updates that player's assigned color, and broadcasts metadata upsert with the new color.
    - Test 3: host rejects spoofing by deriving the sender from peer connection, not from client-controlled metadata.
  </behavior>
  <action>
    Add failing tests with these exact names:
    - `UpdatePresenceColorOperation_RoundTrips_ThroughMessagePack`
    - `GameHost_UpdatePresenceColorOperation_UpdatesPlayerRosterAndBroadcastsPeerMetadata`

    Then implement the protocol exactly:
    1. In `src/BFGA.Network/Protocol/BoardOperation.cs`
       - add new `OperationType.UpdatePresenceColor`
       - add new union entry after existing laser operation entries using the next available union key
       - add:
         ```csharp
         [MessagePackObject]
         public sealed class UpdatePresenceColorOperation : BoardOperation
         {
             public override OperationType Type => OperationType.UpdatePresenceColor;
             [Key(3)] public SKColor Color { get; set; }
             public UpdatePresenceColorOperation() { }
             public UpdatePresenceColorOperation(SKColor color) { Color = color; }
         }
         ```

    2. In `src/BFGA.Network/GameHost.cs`
       - allow `UpdatePresenceColorOperation` through `ValidateOperation(...)` only for known connected peers
       - inside `HandleOperation(...)`, before generic board mutation path, handle `UpdatePresenceColorOperation updateColor` by:
         - looking up `operation.SenderId` in `_players`
         - replacing stored `PlayerInfo` with same `DisplayName` and `updateColor.Color`
         - broadcasting `new PeerJoinedOperation(operation.SenderId, updatedDisplayName, updateColor.Color)` reliably
         - returning without touching board state or undo/redo
       - do not trust any client-supplied display name or sender id for the visible update; sender must come from `GetClientId(peer)` only

    This task implements D-01/D-02 with host authority and preserves D-08 by not changing disconnect flow.
  </action>
  <verify>
    <automated>dotnet test tests/BFGA.Network.Tests --filter "FullyQualifiedName~ProtocolTests|FullyQualifiedName~NetworkTests" --no-restore -v q</automated>
  </verify>
  <acceptance_criteria>
    - `src/BFGA.Network/Protocol/BoardOperation.cs` contains `UpdatePresenceColor`
    - `src/BFGA.Network/Protocol/BoardOperation.cs` contains `class UpdatePresenceColorOperation`
    - `src/BFGA.Network/GameHost.cs` contains `case UpdatePresenceColorOperation`
    - `src/BFGA.Network/GameHost.cs` contains `new PeerJoinedOperation(operation.SenderId`
    - `dotnet test tests/BFGA.Network.Tests --filter "FullyQualifiedName~ProtocolTests|FullyQualifiedName~NetworkTests" --no-restore -v q` exits 0
  </acceptance_criteria>
  <done>Connected clients can request a new presence color through a host-authoritative protocol path and the host broadcasts the canonical metadata update.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: sync preferred presence color into roster, cursor, and remote laser identity</name>
  <files>tests/BFGA.App.Tests/MainViewModelTests.cs, tests/BFGA.App.Tests/RosterOverlayTests.cs, src/BFGA.App/ViewModels/MainViewModel.cs</files>
  <read_first>
    - src/BFGA.App/ViewModels/MainViewModel.cs
    - tests/BFGA.App.Tests/MainViewModelTests.cs
    - tests/BFGA.App.Tests/RosterOverlayTests.cs
    - src/BFGA.App/Views/RosterOverlay.axaml
    - .planning/phases/05-polish-configuration/05-CONTEXT.md
    - .planning/phases/05-polish-configuration/05-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: host changing `LaserPresenceColor` broadcasts a pseudo-join metadata update for `Guid.Empty` so remote clients learn host identity.
    - Test 2: client full sync sends `UpdatePresenceColorOperation` when preferred color differs from roster-assigned local color.
    - Test 3: changing preferred color while connected as client sends `UpdatePresenceColorOperation` immediately.
    - Test 4: inbound `PeerJoinedOperation` refreshes roster/cursor/remote laser identity with the new color and disconnect still removes the peer's laser immediately.
  </behavior>
  <action>
    Add failing tests with these exact names:
    - `MainViewModel_HostLaserPresenceColorChange_BroadcastsHostMetadataUpsert`
    - `MainViewModel_ClientFullSync_WhenPreferredPresenceColorDiffers_SendsUpdatePresenceColorOperation`
    - `MainViewModel_ClientLaserPresenceColorChange_SendsUpdatePresenceColorOperation`
    - `PeerJoinedOperation_ColorRefresh_RecolorsRemoteIdentityAndPeerLeftStillRemovesLaser`

    Then update `src/BFGA.App/ViewModels/MainViewModel.cs` exactly:
    1. add private helper:
       ```csharp
       private void SyncPreferredPresenceColor()
       ```
       Behavior:
       - if `Host is not null`, call `Host.BroadcastOperation(new PeerJoinedOperation(Guid.Empty, DisplayName, LaserPresenceColor), reliable: true);`
       - else if `Client is not null && _localClientId.HasValue`, compare `GetPlayerInfo(_localClientId.Value)?.AssignedColor` to `LaserPresenceColor`; if different send `Client.SendOperation(new UpdatePresenceColorOperation(LaserPresenceColor));`

    2. call `SyncPreferredPresenceColor()` in these places:
       - end of `LaserPresenceColor` setter after saving settings
       - end of `ApplyFullSync(...)` after `Roster = ...` and `ReconcileRemoteState()`
       - `OnPeerJoined(...)` when `Host is not null` so each newly connected client also receives host pseudo-join metadata

    3. keep existing `PeerJoinedOperation` inbound handling as roster upsert. Do not add a second color-only inbound code path.

    4. preserve `RemoveRosterEntry(...)` immediate laser removal exactly as D-08 requires.
  </action>
  <verify>
    <automated>dotnet test tests/BFGA.App.Tests --filter "FullyQualifiedName~MainViewModelTests|FullyQualifiedName~RosterOverlayTests" --no-restore -v q</automated>
  </verify>
  <acceptance_criteria>
    - `src/BFGA.App/ViewModels/MainViewModel.cs` contains `private void SyncPreferredPresenceColor()`
    - `src/BFGA.App/ViewModels/MainViewModel.cs` contains `new UpdatePresenceColorOperation(LaserPresenceColor)`
    - `src/BFGA.App/ViewModels/MainViewModel.cs` contains `new PeerJoinedOperation(Guid.Empty, DisplayName, LaserPresenceColor)`
    - `src/BFGA.App/ViewModels/MainViewModel.cs` still contains `lasers.Remove(clientId);`
    - `dotnet test tests/BFGA.App.Tests --filter "FullyQualifiedName~MainViewModelTests|FullyQualifiedName~RosterOverlayTests" --no-restore -v q` exits 0
  </acceptance_criteria>
  <done>Preferred color now drives live peer-visible presence identity for host and client sessions while disconnect cleanup remains immediate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client preference -> host roster | user-selected color crosses network trust boundary |
| host metadata -> remote identity UI | authoritative roster color becomes peer-visible roster/cursor/laser state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Spoofing | `UpdatePresenceColorOperation` | mitigate | derive sender from `GetClientId(peer)` and update only that player's `PlayerInfo`; never trust client-controlled identity metadata |
| T-05-05 | Tampering | host metadata broadcast | mitigate | broadcast canonical `PeerJoinedOperation` from host after updating roster state so clients consume a trusted color source |
| T-05-06 | Denial of Service | disconnect cleanup | mitigate | preserve `RemoveRosterEntry(...)->lasers.Remove(clientId)` immediate cleanup path; no deferred fade on peer leave |
</threat_model>

<verification>
1. `dotnet test tests/BFGA.Network.Tests --filter "FullyQualifiedName~ProtocolTests|FullyQualifiedName~NetworkTests" --no-restore -v q`
2. `dotnet test tests/BFGA.App.Tests --filter "FullyQualifiedName~MainViewModelTests|FullyQualifiedName~RosterOverlayTests" --no-restore -v q`
3. grep `class UpdatePresenceColorOperation` in `src/BFGA.Network/Protocol/BoardOperation.cs`
4. grep `SyncPreferredPresenceColor` in `src/BFGA.App/ViewModels/MainViewModel.cs`
</verification>

<success_criteria>
- Preferred color changes converge into host-authoritative roster metadata
- Roster, cursor, and remote laser color identity stay aligned
- Host identity no longer falls back to anonymous white on remote clients
- Peer disconnect still removes laser state immediately
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-configuration/05-02-SUMMARY.md`
</output>
