---
phase: 04-multiplayer-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/BFGA.App/Views/BoardView.axaml.cs
  - tests/BFGA.App.Tests/BoardViewPipelineTests.cs
autonomous: true
requirements:
  - MULT-01

must_haves:
  truths:
    - "Press, drag, release, and cancel on the local laser tool publish network laser operations in real time"
    - "Release and cancel send an inactive laser update so remote peers stop appending points and begin fade-out"
    - "Quick tap still creates only the local ping visual; multiplayer path stays laser-op only"
  artifacts:
    - path: "src/BFGA.App/Views/BoardView.axaml.cs"
      provides: "Local laser gesture publishes LaserPointerOperation through MainViewModel"
      contains: "PublishLocalBoardOperation(new LaserPointerOperation"
    - path: "tests/BFGA.App.Tests/BoardViewPipelineTests.cs"
      provides: "Regression coverage for begin/update/complete/cancel network emission"
      contains: "LaserPointer_BeginLocalLaser_PublishesActiveOperation"
  key_links:
    - from: "BeginLocalLaser"
      to: "MainViewModel.PublishLocalBoardOperation"
      via: "active laser op on pointer press"
      pattern: "new LaserPointerOperation"
    - from: "CompleteLocalLaser"
      to: "MainViewModel.PublishLocalBoardOperation"
      via: "inactive laser op on release"
      pattern: "IsActive = false"
---

<objective>
Publish local laser activity from `BoardView` into the existing multiplayer protocol so every gesture begins, streams, and ends over `LaserPointerOperation` without touching board state.

Purpose: MULT-01 cannot pass while the laser tool remains local-only. This plan turns the existing Phase 3 gesture path into the authoritative send path for host and client sessions.

Output: `BoardView` gesture helpers publish active/inactive laser operations and app tests prove remote-visible emission semantics.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-multiplayer-integration/04-CONTEXT.md
@.planning/phases/04-multiplayer-integration/04-RESEARCH.md
@.planning/phases/03-local-tool-implementation/03-03-SUMMARY.md

<interfaces>
From src/BFGA.App/Views/BoardView.axaml.cs:
```csharp
private void BeginLocalLaser(Vector2 boardPoint, Point screenPoint, long timestampMs)
private void UpdateLocalLaser(Vector2 boardPoint, long timestampMs)
private void CompleteLocalLaser(Vector2 boardPoint, Point screenPoint, long timestampMs)
private void CancelLocalLaser()
```

From src/BFGA.App/ViewModels/MainViewModel.cs:
```csharp
public Task PublishLocalBoardOperation(BoardOperation operation)
```

From src/BFGA.Network/Protocol/BoardOperation.cs:
```csharp
public class LaserPointerOperation : BoardOperation
{
    [Key(3)] public Vector2 Position { get; set; }
    [Key(4)] public bool IsActive { get; set; }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: publish local laser operations from BoardView gesture helpers</name>
  <files>tests/BFGA.App.Tests/BoardViewPipelineTests.cs, src/BFGA.App/Views/BoardView.axaml.cs</files>
  <read_first>
    - src/BFGA.App/Views/BoardView.axaml.cs
    - src/BFGA.App/ViewModels/MainViewModel.cs
    - tests/BFGA.App.Tests/BoardViewPipelineTests.cs
    - .planning/phases/04-multiplayer-integration/04-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: `BeginLocalLaser` publishes one active `LaserPointerOperation` with current board point.
    - Test 2: `UpdateLocalLaser` publishes active updates only when board point changed.
    - Test 3: `CompleteLocalLaser` publishes one inactive `LaserPointerOperation` at the final board point.
    - Test 4: `CancelLocalLaser` publishes one inactive `LaserPointerOperation` without creating a ping.
    - Test 5: quick tap still creates local ping and publishes only laser active/inactive ops, not a separate ping network op.
  </behavior>
  <action>
    Add failing regression tests in `tests/BFGA.App.Tests/BoardViewPipelineTests.cs` with these exact names:
    - `LaserPointer_BeginLocalLaser_PublishesActiveOperation`
    - `LaserPointer_UpdateLocalLaser_ChangedPointPublishesActiveOperation`
    - `LaserPointer_CompleteLocalLaser_PublishesInactiveOperation`
    - `LaserPointer_CancelLocalLaser_PublishesInactiveOperation`
    - `LaserPointer_QuickTap_KeepsLocalPingAndPublishesLaserLifecycleOnly`

    Use existing fake-session / reflection patterns already present in app tests. Drive the existing private helpers directly.

    Then update `src/BFGA.App/Views/BoardView.axaml.cs` so the local gesture helpers publish exact payloads through `_boardScreenViewModel!.MainViewModel.PublishLocalBoardOperation(...)`:
    - `BeginLocalLaser(...)` publishes `new LaserPointerOperation(Guid.Empty, boardPoint, true)` immediately after local state initialized.
    - `UpdateLocalLaser(...)` publishes `new LaserPointerOperation(Guid.Empty, boardPoint, true)` only when `boardPoint` differs from the last trail point. Do not publish duplicate stationary updates; this is required to mitigate pointer-move DoS volume.
    - `CompleteLocalLaser(...)` publishes `new LaserPointerOperation(Guid.Empty, boardPoint, false)` after local state marked inactive.
    - `CancelLocalLaser()` publishes `new LaserPointerOperation(Guid.Empty, LocalLaser.HeadPosition, false)` when `LocalLaser` exists.

    Preserve Phase 3 behavior:
    - keep local ping creation untouched
    - do not clear local trail on release/cancel
    - do not publish any board mutation op
    - do not add a new network message type for ping

    Implement per D-05 and D-06: remote peers learn release/cancel exclusively through `IsActive=false`, then fade existing trail naturally.
  </action>
  <verify>
    <automated>dotnet test tests/BFGA.App.Tests --filter "FullyQualifiedName~BoardViewPipelineTests" --no-restore -v q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/BFGA.App.Tests/BoardViewPipelineTests.cs` contains `LaserPointer_BeginLocalLaser_PublishesActiveOperation`
    - `tests/BFGA.App.Tests/BoardViewPipelineTests.cs` contains `LaserPointer_CancelLocalLaser_PublishesInactiveOperation`
    - `src/BFGA.App/Views/BoardView.axaml.cs` contains `PublishLocalBoardOperation(new LaserPointerOperation(Guid.Empty, boardPoint, true))`
    - `src/BFGA.App/Views/BoardView.axaml.cs` contains `PublishLocalBoardOperation(new LaserPointerOperation(Guid.Empty, boardPoint, false))`
    - `src/BFGA.App/Views/BoardView.axaml.cs` contains `LocalLaser.HeadPosition`
    - `dotnet test tests/BFGA.App.Tests --filter "FullyQualifiedName~BoardViewPipelineTests" --no-restore -v q` exits 0
  </acceptance_criteria>
  <done>Local laser gestures emit protocol updates in real time, release/cancel propagate as inactive laser state, and existing local visual behavior remains intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pointer input -> network publish | high-frequency UI input becomes multiplayer traffic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Denial of Service | `BoardView.UpdateLocalLaser` | mitigate | publish active ops only when board point changes; no duplicate stationary sends |
| T-04-02 | Spoofing | `LaserPointerOperation.SenderId` | mitigate | use existing `PublishLocalBoardOperation` path so host/client stamp authoritative sender id and laser stays in existing trusted protocol |
</threat_model>

<verification>
1. `dotnet test tests/BFGA.App.Tests --filter "FullyQualifiedName~BoardViewPipelineTests" --no-restore -v q`
2. grep `PublishLocalBoardOperation(new LaserPointerOperation(Guid.Empty, boardPoint, true))` in `src/BFGA.App/Views/BoardView.axaml.cs`
3. grep `LaserPointer_CompleteLocalLaser_PublishesInactiveOperation` in `tests/BFGA.App.Tests/BoardViewPipelineTests.cs`
</verification>

<success_criteria>
- Local press/drag/release/cancel emits multiplayer laser operations without board mutation
- Remote peers receive explicit inactive signal on release/cancel
- Quick tap preserves local ping UX and does not invent a separate ping network protocol
</success_criteria>

<output>
After completion, create `.planning/phases/04-multiplayer-integration/04-01-SUMMARY.md`
</output>
