---
phase: 04-multiplayer-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/BFGA.App/ViewModels/MainViewModel.cs
  - tests/BFGA.App.Tests/MainViewModelTests.cs
autonomous: true
requirements:
  - MULT-01
  - MULT-02
  - MULT-03

must_haves:
  truths:
    - "Remote laser state always uses the peer's roster color, not a payload-specific color"
    - "Inactive remote laser updates stop new point append but preserve existing trail for fade-out"
    - "Full sync and peer leave remove stale remote lasers while preserving valid peers"
    - "Multiple peers can hold independent remote laser state concurrently"
  artifacts:
    - path: "src/BFGA.App/ViewModels/MainViewModel.cs"
      provides: "Roster-aware remote laser reconciliation and cleanup"
      contains: "RebuildRemoteLasers"
    - path: "tests/BFGA.App.Tests/MainViewModelTests.cs"
      provides: "Regression coverage for laser color, release fade, cleanup, and multi-peer independence"
      contains: "LaserPointerOperation_RemotePeer_UsesAssignedRosterColor"
  key_links:
    - from: "ApplyInboundOperation"
      to: "UpsertRemoteLaser"
      via: "LaserPointerOperation case"
      pattern: "case LaserPointerOperation laserOp"
    - from: "ReconcileRemoteState"
      to: "RemoteLasers"
      via: "roster/full-sync cleanup"
      pattern: "RemoteLasers = RebuildRemoteLasers"
---

<objective>
Make remote laser state lifecycle roster-authoritative and cleanup-safe so multiplayer lasers keep correct per-user color, survive fade-out correctly, and disappear when a peer no longer exists.

Purpose: MULT-01/02/03 fail if remote laser state drifts from roster color, keeps ghost peers after full sync, or cross-contaminates simultaneous users.

Output: `MainViewModel` remote laser reconciliation logic and regression tests for color identity, fade behavior, cleanup, and simultaneous peers.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-multiplayer-integration/04-CONTEXT.md
@.planning/phases/04-multiplayer-integration/04-RESEARCH.md
@.planning/phases/02-trail-buffer-renderer/02-02-SUMMARY.md

<interfaces>
From src/BFGA.App/ViewModels/MainViewModel.cs:
```csharp
private void ApplyInboundOperation(BoardOperation operation)
private void UpsertRemoteLaser(LaserPointerOperation operation)
private void ReconcileRemoteState()
private PlayerInfo? GetPlayerInfo(Guid clientId)
private bool ShouldIgnoreLocalPresence(Guid clientId)
```

From src/BFGA.Canvas/Rendering/RemoteLaserState.cs:
```csharp
public sealed class RemoteLaserState
{
    public LaserTrailBuffer Trail { get; }
    public bool IsActive { get; set; }
    public long LastUpdateMs { get; set; }
    public SKColor Color { get; set; }
}
```

From src/BFGA.Network/PlayerInfo.cs:
```csharp
public SKColor AssignedColor { get; set; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: reconcile remote laser state against roster and fade semantics</name>
  <files>tests/BFGA.App.Tests/MainViewModelTests.cs, src/BFGA.App/ViewModels/MainViewModel.cs</files>
  <read_first>
    - src/BFGA.App/ViewModels/MainViewModel.cs
    - src/BFGA.Canvas/Rendering/RemoteLaserState.cs
    - src/BFGA.Network/PlayerInfo.cs
    - tests/BFGA.App.Tests/MainViewModelTests.cs
    - .planning/phases/04-multiplayer-integration/04-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: remote laser upsert uses `PlayerInfo.AssignedColor` per D-01/D-02.
    - Test 2: inactive laser update keeps existing trail count while marking state inactive per D-05/D-06.
    - Test 3: full sync reconciles placeholder laser color and removes senders not present in roster.
    - Test 4: peer leave removes remote laser state immediately.
    - Test 5: multiple remote peers keep independent trail buffers and colors.
  </behavior>
  <action>
    Add failing regression tests in `tests/BFGA.App.Tests/MainViewModelTests.cs` with these exact names:
    - `LaserPointerOperation_RemotePeer_UsesAssignedRosterColor`
    - `LaserPointerOperation_InactiveUpdate_PreservesTrailForFade`
    - `FullSync_ReconcilesRemoteLaserColorAndDropsUnknownPeers`
    - `PeerLeft_RemovesRemoteLaserState`
    - `LaserPointerOperation_MultiplePeers_KeepIndependentLaserState`

    Then update `src/BFGA.App/ViewModels/MainViewModel.cs` with exact roster-authoritative behavior:

    1. In `UpsertRemoteLaser(LaserPointerOperation operation)`:
       - keep `ShouldIgnoreLocalPresence(operation.SenderId)` early return
       - resolve `playerInfo = GetPlayerInfo(operation.SenderId)`
       - create new state with `new RemoteLaserState(playerInfo?.AssignedColor ?? SkiaSharp.SKColors.White)` when missing
       - always set `state.Color = playerInfo?.AssignedColor ?? state.Color` on every update so placeholder white upgrades to roster color once known
       - always set `state.IsActive = operation.IsActive` and `state.LastUpdateMs = Environment.TickCount64`
       - append `operation.Position` only when `operation.IsActive` is true
       - do not clear `state.Trail` when `operation.IsActive` is false

    2. Add a new private helper with this exact name:
       ```csharp
       private Dictionary<Guid, RemoteLaserState> RebuildRemoteLasers(HashSet<Guid> validIds)
       ```
       Behavior:
       - keep only senders in `validIds`
       - preserve existing `Trail`, `IsActive`, and `LastUpdateMs`
       - refresh `Color` from `GetPlayerInfo(entry.Key)?.AssignedColor ?? entry.Value.Color`

    3. In `ReconcileRemoteState()`:
       - when roster empty, also set `RemoteLasers = new Dictionary<Guid, RemoteLaserState>()`
       - after `RemoteStrokePreviews = RebuildRemoteStrokePreviews(validIds);`, add `RemoteLasers = RebuildRemoteLasers(validIds);`

    4. Keep existing host palette rules from `GameHost.PlayerColors` untouched per D-07/D-08. Do not add new contrast logic, payload color, or negotiation flow.
  </action>
  <verify>
    <automated>dotnet test tests/BFGA.App.Tests --filter "FullyQualifiedName~MainViewModelTests" --no-restore -v q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/BFGA.App.Tests/MainViewModelTests.cs` contains `LaserPointerOperation_RemotePeer_UsesAssignedRosterColor`
    - `tests/BFGA.App.Tests/MainViewModelTests.cs` contains `LaserPointerOperation_InactiveUpdate_PreservesTrailForFade`
    - `src/BFGA.App/ViewModels/MainViewModel.cs` contains `private Dictionary<Guid, RemoteLaserState> RebuildRemoteLasers(HashSet<Guid> validIds)`
    - `src/BFGA.App/ViewModels/MainViewModel.cs` contains `state.Color = playerInfo?.AssignedColor ?? state.Color`
    - `src/BFGA.App/ViewModels/MainViewModel.cs` contains `RemoteLasers = RebuildRemoteLasers(validIds);`
    - `dotnet test tests/BFGA.App.Tests --filter "FullyQualifiedName~MainViewModelTests" --no-restore -v q` exits 0
  </acceptance_criteria>
  <done>Remote laser state follows roster color identity, release/cancel fades naturally, stale peers are purged on leave/full sync, and simultaneous peers stay isolated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inbound network op -> presence state | untrusted peer laser updates mutate on-screen collaborator state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Spoofing | remote laser color identity | mitigate | ignore payload-derived identity and refresh `RemoteLaserState.Color` from roster `PlayerInfo.AssignedColor` on every reconciliation/update |
| T-04-04 | Denial of Service | ghost remote laser state | mitigate | purge missing peer IDs in `RebuildRemoteLasers` and clear all lasers when roster resets/full sync removes peers |
</threat_model>

<verification>
1. `dotnet test tests/BFGA.App.Tests --filter "FullyQualifiedName~MainViewModelTests" --no-restore -v q`
2. grep `RebuildRemoteLasers` in `src/BFGA.App/ViewModels/MainViewModel.cs`
3. grep `LaserPointerOperation_RemotePeer_UsesAssignedRosterColor` in `tests/BFGA.App.Tests/MainViewModelTests.cs`
</verification>

<success_criteria>
- Remote lasers always render with roster-assigned peer color
- Release/cancel updates stop new point append and preserve fade trail
- Full sync and disconnect remove stale remote laser entries
- Multiple peers retain independent remote laser state concurrently
</success_criteria>

<output>
After completion, create `.planning/phases/04-multiplayer-integration/04-02-SUMMARY.md`
</output>
